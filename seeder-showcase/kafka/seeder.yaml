seeder:
  state:
    - name: kafka seed json file
      type: kafka
      config:
        - file: seed/seed.json

    - name: kafka seed yml file
      type: kafka
      config:
        - file: seed/seed.yml
